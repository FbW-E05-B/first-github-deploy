{
  "name": "deploy",
  "version": "1.0.0",
  "description": "exercise for deploying website",
  "scripts": {
    "start": "parcel index.html",
    "build": "rm -rf dist && parcel build index.html --public-url ./",
    "publish": "gh-pages -d dist"
  },
  "author": "Dewan",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}
